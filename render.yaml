services:
  - type: web
    name: payu-integration-hub
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS
        value: multiflow.onrender.com,.onrender.com

